// Simulação básica de InputMask
window.Inputmask = function (mask) {
    return {
        mask: function (selector) {
            console.log(`Aplicando máscara "${mask}" no elemento ${selector}`);
        }
    };
};
